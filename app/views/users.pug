extends layout

block content
  include header

  h2 Current list of users

  ul
  each user in users
    li #{user.username} (#{user.email})
  else
    li There are no user

  script(src="js/refresh.js")
